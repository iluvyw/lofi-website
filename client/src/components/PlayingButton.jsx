import React from 'react'
import { useState } from 'react'
export default function PlayingButton({play, pause, init}) {
    const [isPlaying,setIsPlaying] = useState(init)
    
    return (
        <div>
            
        </div>
    )
}
